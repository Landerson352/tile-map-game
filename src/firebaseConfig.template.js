const firebaseConfig = {
  // Duplicate this file and name it `firebaseConfig.js`
  // Paste `firebaseConfig` from the Project General Settings in the Firebase console here.
};

export default firebaseConfig;